branding:
  hide-footer: true

auth:
  secret-key: ${SECRET_KEY}
  users:
    jakub:
      password-hash: ${PASSWORD_HASH}

server:
  proxied: true

pages:
  !include: home.yml
  !include: server.yml

